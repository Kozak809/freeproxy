# 🚀 FreeProxy - Advanced V2Ray Profile Finder

Мощный инструмент командной строки для поиска и управления бесплатными VPN/прокси серверами с сайта v2nodes.com.

## ✨ Особенности

- 🔍 **Умный поиск** - находит серверы с минимальной задержкой
- 🌍 **Множество стран** - поддержка поиска по нескольким странам одновременно
- 📊 **Фильтрация** - по задержке, протоколу и рейтингу
- 🎨 **Цветной вывод** - наглядная индикация качества соединения
- 📋 **Копирование** - автоматическое копирование в буфер обмена
- 📱 **QR-коды** - генерация QR для мобильных устройств
- 🎲 **Случайный выбор** - для разнообразия
- 🔍 **Глубокий поиск** - сканирование нескольких страниц
- 🎯 **Интерактивный режим** - удобный выбор из списка
- ✅ **Проверка доступности** - тестирование серверов

## 📦 Установка

### Быстрая установка

```bash
# Скачать скрипт
curl -o freeproxy https://raw.githubusercontent.com/your-repo/freeproxy/main/freeproxy

# Сделать исполняемым
chmod +x freeproxy

# Переместить в PATH
sudo mv freeproxy /usr/local/bin/

# Проверить установку
freeproxy -h
```

### Установка зависимостей (опционально)

Для полного функционала установите дополнительные утилиты:

```bash
# Для копирования в буфер обмена
sudo apt install xclip  # Debian/Ubuntu
# или
sudo pacman -S xclip    # Arch Linux
# или
brew install xclip      # macOS (также можно использовать pbcopy)

# Для генерации QR-кодов
sudo apt install qrencode  # Debian/Ubuntu
sudo pacman -S qrencode    # Arch Linux
brew install qrencode      # macOS
```

## 🎯 Использование

### Базовые примеры

```bash
# Найти лучший сервер в России
freeproxy ru

# Найти топ-5 серверов
freeproxy ru -r 5

# Глубокий поиск на 3 страницах
freeproxy ru -d 3

# Комбинация: топ-3 из 2 страниц
freeproxy ru -d 2 -r 3
```

### Работа с несколькими странами

```bash
# Лучший сервер из трёх стран
freeproxy ru,us,de

# Топ-5 из нескольких стран
freeproxy ru,us,de,fr -r 5
```

### Фильтрация

```bash
# Только серверы с задержкой менее 100ms
freeproxy ru -m 100

# Поиск серверов Trojan
freeproxy ru -p trojan

# Поиск VMess серверов
freeproxy ru -p vmess

# Все типы протоколов
freeproxy ru -p all
```

### Экспорт и копирование

```bash
# Копировать первую ссылку в буфер обмена
freeproxy ru -c

# Сохранить топ-10 в файл
freeproxy ru -r 10 -o servers.txt

# Копировать и сохранить одновременно
freeproxy ru -r 5 -c -o backup.txt
```

### Интерактивный режим

```bash
# Выбрать сервер из списка
freeproxy ru -i

# Интерактивный выбор с подробной информацией
freeproxy ru -d 2 -r 5 -i -v
```

### QR-коды для мобильных

```bash
# Показать QR-код для первого сервера
freeproxy ru --qr

# QR + копирование
freeproxy ru -c --qr
```

### Проверка и отладка

```bash
# Подробная информация о серверах
freeproxy ru -v

# Проверить доступность серверов
freeproxy ru --test

# Всё вместе
freeproxy ru -r 3 -v --test
```

### Случайный выбор

```bash
# Случайный сервер из топ-10
freeproxy ru --random

# Случайный из нескольких стран
freeproxy ru,us,de --random
```

## 🎨 Цветовая индикация задержки

- 🟢 **Зелёный** (<100ms) - Отличное соединение
- 🟡 **Жёлтый** (100-300ms) - Приемлемое соединение
- 🔴 **Красный** (>300ms) - Медленное соединение

## 📖 Полный список параметров

### Основные параметры

| Параметр | Описание | Пример |
|----------|----------|--------|
| `<коды_стран>` | Код страны или список через запятую | `ru`, `ru,us,de` |

### Опции поиска

| Опция | Краткая | Описание | Пример |
|-------|---------|----------|--------|
| `--deep <N>` | `-d` | Количество страниц для поиска | `-d 3` |
| `--rating <N>` | `-r` | Топ N лучших серверов | `-r 5` |
| `--max-latency <N>` | `-m` | Максимальная задержка (ms) | `-m 150` |
| `--protocol <TYPE>` | `-p` | Тип протокола (vless/vmess/trojan/all) | `-p trojan` |

### Режимы вывода

| Опция | Краткая | Описание |
|-------|---------|----------|
| `--copy` | `-c` | Копировать в буфер обмена |
| `--output <FILE>` | `-o` | Сохранить в файл |
| `--verbose` | `-v` | Подробная информация |
| `--interactive` | `-i` | Интерактивный выбор |
| `--qr` | | Показать QR-код |
| `--random` | | Случайный выбор |

### Дополнительно

| Опция | Описание |
|-------|----------|
| `--test` | Проверить доступность (ping) |
| `--help` | Показать справку |

## 💡 Продвинутые примеры

### Идеальный сервер для стриминга
```bash
freeproxy ru,de,nl -m 80 -d 3 -r 1 -c --test
```
Найти самый быстрый сервер (<80ms) из трёх стран, проверить доступность и скопировать.

### Создание списка резервных серверов
```bash
freeproxy ru -d 5 -r 20 -m 150 -o backup_servers.txt
```
Собрать 20 серверов с хорошей задержкой, проверив 5 страниц.

### Быстрый случайный выбор
```bash
freeproxy us,uk,ca --random -c --qr
```
Выбрать случайный сервер, скопировать и показать QR.

### Интерактивный выбор лучших
```bash
freeproxy ru,de -d 2 -r 10 -i -v --test
```
Показать топ-10, дать выбрать интерактивно с полной информацией.

### Поиск специфического протокола
```bash
freeproxy ru -p trojan -d 3 -m 100 -r 5 -o trojan_servers.txt
```
Найти топ-5 Trojan серверов с низкой задержкой.

## ⚠️ Важные замечания

### О безопасности

- ⚠️ **Бесплатные прокси НЕ гарантируют приватность**
- Владелец VPS может видеть ваш реальный IP и посещаемые сайты
- Используйте только для обхода блокировок, **НЕ** для конфиденциальных данных
- Всегда используйте HTTPS сайты (замочек в браузере)
- Не вводите пароли и личные данные через бесплатные прокси

### Что владелец сервера может видеть:

✅ **Может видеть:**
- Ваш реальный IP-адрес
- Домены, которые вы посещаете
- DNS-запросы
- Незашифрованный трафик (HTTP)
- Время и объём трафика

❌ **НЕ может видеть (при HTTPS):**
- Содержимое страниц
- Пароли и логины
- Данные форм
- Конкретные URL страниц

### Рекомендации

1. **Для обхода блокировок** - используйте бесплатные прокси
2. **Для приватности** - используйте платные VPN (Mullvad, ProtonVPN)
3. **Для анонимности** - используйте Tor Browser
4. **Всегда проверяйте** HTTPS соединение на важных сайтах

## 🔧 Устранение неполадок

### Команда не найдена

```bash
# Очистить кэш bash
hash -r

# Проверить расположение
which freeproxy

# Проверить PATH
echo $PATH
```

### Не работает копирование

```bash
# Установить xclip
sudo apt install xclip

# Или xsel
sudo apt install xsel
```

### Не работает QR-код

```bash
# Установить qrencode
sudo apt install qrencode
```

### Медленный поиск

```bash
# Используйте меньше страниц
freeproxy ru -d 1

# Или укажите максимальную задержку
freeproxy ru -m 200
```

## 🤝 Коды стран

Популярные коды стран для поиска:

| Код | Страна | Код | Страна |
|-----|--------|-----|--------|
| `ru` | 🇷🇺 Россия | `us` | 🇺🇸 США |
| `de` | 🇩🇪 Германия | `uk` | 🇬🇧 Великобритания |
| `fr` | 🇫🇷 Франция | `nl` | 🇳🇱 Нидерланды |
| `ca` | 🇨🇦 Канада | `sg` | 🇸🇬 Сингапур |
| `jp` | 🇯🇵 Япония | `kr` | 🇰🇷 Южная Корея |
| `pl` | 🇵🇱 Польша | `tr` | 🇹🇷 Турция |

Полный список доступен на [v2nodes.com](https://www.v2nodes.com)

## 📝 Примеры сценариев использования

### Ежедневная ротация серверов

```bash
#!/bin/bash
# daily_vpn.sh - Ежедневно получать новый случайный сервер

freeproxy ru,de,nl --random -c | tee ~/current_vpn.txt
echo "Новый VPN профиль скопирован в буфер обмена"
```

### Найти лучший сервер для игр

```bash
freeproxy ru,de,pl -m 50 -d 3 --test -i
```

### Создать базу серверов для путешествий

```bash
#!/bin/bash
# travel_vpns.sh

countries=("us" "uk" "de" "fr" "es" "it" "jp" "au")

for country in "${countries[@]}"; do
    echo "Собираю серверы для $country..."
    freeproxy "$country" -r 3 -o "vpn_${country}.txt"
done
```

## 📊 FAQ

**Q: Насколько безопасны эти серверы?**  
A: Они подходят только для обхода блокировок, НЕ для защиты данных.

**Q: Почему некоторые серверы не работают?**  
A: Бесплатные серверы часто перегружены или временно недоступны.

**Q: Можно ли использовать для торрентов?**  
A: Не рекомендуется, так как владелец видит весь трафик.

**Q: Как часто обновляются серверы?**  
A: На v2nodes.com серверы обновляются постоянно, используйте свежие результаты.

**Q: Какой протокол лучше выбрать?**  
A: VLESS с Reality обычно наиболее стабилен, Trojan тоже хорош.

## 📜 Лицензия

MIT License - свободное использование и модификация.

## 🙏 Благодарности

- [v2nodes.com](https://www.v2nodes.com) за предоставление списка серверов
- Сообщество V2Ray за разработку протоколов

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Попробуйте с флагом `-v` для подробной информации

---

**Помните**: Это инструмент для обхода блокировок, а не для обеспечения приватности! 🔒
